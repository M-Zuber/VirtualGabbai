namespace Framework
{
    public static class Enums
    {
        public enum CRUDResults
        {
            ReadSuccess,
            ReadFail,
            CreateSuccess,
            CreateFail,
            UpdateSuccess,
            UpdateFail,
            DeleteSuccess,
            DeleteFail
        }
    }
}
